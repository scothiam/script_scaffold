# Core runtime dependency used by the ORM and repositories
SQLAlchemy>=2.0

# Optional: RSS/Atom crawling support
feedparser>=6.0

# Optional: search integrations (search.py)
openai>=1.0
anthropic>=0.25
tavily-python>=0.3
duckduckgo-search>=5.0

# Optional: LangChain provider factory (llm.py — install only what you use)
langchain-openai>=0.1
langchain-anthropic>=0.1
langchain-ollama>=0.1
